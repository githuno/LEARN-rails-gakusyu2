%h1.text-center.font-weight-bold.mb-4.border-bottom 全体タイムライン

.d-flex.align-items-center.gap-2
  = link_to '新規投稿', new_post_path, class: 'btn btn-primary mb-4'
  - if user_signed_in?
    = link_to 'マイページ', user_path(current_user), class: 'btn btn-secondary mb-4'

.posts.d-flex.flex-row.overflow-auto
  - @posts.each do |post|
    .card.mr-2(style="width: 18rem;")
      .card-body
        %h5.card-title
          = link_to post.user.username, user_path(post.user)
        %p.card-text
          = post.content
        %p.card-text
          = post.created_at
        - if user_signed_in? && current_user != post.user
          - if current_user.following?(post.user)
            = button_to 'フォロー解除', unfollow_user_path(post.user), method: :delete, class: 'btn btn-danger'
          - else
            = button_to 'フォロー', follow_user_path(post.user), method: :post, class: 'btn btn-primary'